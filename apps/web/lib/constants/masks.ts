export const MASK_GRADIENTS = {
  fadeBottom: 'linear-gradient(to bottom, transparent, transparent, transparent, white)',
  fadeTop: 'linear-gradient(to top, transparent, transparent, transparent, white)',
} as const;

